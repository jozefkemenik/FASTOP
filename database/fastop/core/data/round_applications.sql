/* Formatted on 11/29/2019 11:43:30 (QP5 v5.252.13127.32847) */
SET DEFINE OFF;

DECLARE
   l_app_sid   NUMBER (8);
BEGIN
   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'SCP';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (60, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (49, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (3, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (87, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (74, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (2, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (1, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (18, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'FGD';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'DBP';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (78, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (63, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (52, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (34, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (4, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (89, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (97, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'NFR';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'IFI';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'MTBF';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);

   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'GBD';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (94, l_app_sid);

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (104, l_app_sid);


   SELECT A.APP_SID
     INTO l_app_sid
     FROM APPLICATIONS A
    WHERE A.APP_ID = 'PIM';

   INSERT INTO ROUND_APPLICATIONS (ROUND_SID, APP_SID)
        VALUES (260, l_app_sid);
   COMMIT;
END;
